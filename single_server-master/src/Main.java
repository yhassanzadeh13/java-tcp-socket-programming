public class Main
{
    public static void main(String args[])
    {
        /*
         * Creating a server socket on the default server port
         */
        SingleServer singleServer = new SingleServer(SingleServer.DEFAULT_SERVER_PORT);
    }
}
